[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
# Install numpy directly through Pipfile since garage
# requires numpy to be install first.
numpy = "==1.14.5"
meta-agents = {path = "."}

[requires]
python_version = "3.6"
